name: Github Actions Demo 
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Yahoo it works big money moves 🤑"
      - uses: actions/checkout@v3
      - run: npm cli